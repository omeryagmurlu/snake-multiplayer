import 'package:flutter/material.dart';

class NewRoom extends StatelessWidget {
  const NewRoom({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("NewRoom");
  }
}